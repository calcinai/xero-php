<?php

namespace XeroPHP\Models\{{ api.Namespace }};

class {{ class_name }} {

{% for enum in enums %}
{% for enum_value in enum.Values %}
    const {{ enum_value.constant_name }} = '{{ enum_value.value }}';{% if enum.description %} // {{ enum_value.description }}{% endif %}

{% endfor %}

{% endfor %}

}